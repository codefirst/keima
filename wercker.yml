box: node
build:
    steps:
        - npm-install
    after-steps:
        - mzp/http-notify:
            url: $DASHBOZU_URL
deploy:
    steps:
        - heroku-deploy:
            key: $HEROKU_KEY
            user: $HEROKU_USER
            app-name: $HEROKU_APP_NAME

