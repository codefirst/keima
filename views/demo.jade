script(type="text/javascript")
    var app = {
      id : '#{app.id}'
    }
script(src="/javascripts/keima.js")
script(src="/demo.js")

h2 Publish
form#publish
  table
    tr
      th  Channel
      td: input.channel(value="demo")
    tr
      th  Event name
      td: input.name(value="event")
    tr
      th  Sending data
      td: input.data(value="data")
    tr
      th
       td: input.button(type="submit", value="publish")

h2 Subscribe
form#subscribe
  table
    tr
      th Channel
      td: input.channel(value="demo")
    tr
      th
      td: input.button(type="submit", value="subscribe")

h2 Observe
form#observe
  table
    tr
      th Event name
      td: input.name(value="event")
    tr
      th
      td: input.button(type="submit", value="observe")

h2 Console
  p: textarea#console(cols=80,rows=15)

